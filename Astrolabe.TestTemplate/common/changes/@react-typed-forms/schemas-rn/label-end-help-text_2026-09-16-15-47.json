{
  "changes": [
    {
      "packageName": "@react-typed-forms/schemas-rn",
      "comment": "Fix the dropdown renderer throwing `Cannot read properties of undefined (reading 'requiredText')` on a data control that has options but no explicit `renderOptions` - `requiredText` and `portalHost` are now read optionally, matching SelectDataRenderer",
      "type": "patch"
    }
  ],
  "packageName": "@react-typed-forms/schemas-rn"
}
